/**
 * Types Index
 * 
 * Central export point for all type definitions in the application.
 */

// Core slide and storage types
export type {
  TextBox,
  Slide,
  Storage,
  Presence,
  UserMeta,
} from './slide.types';

// Component props types
export type {
  TextBoxProps,
  SlideThumbnailProps,
  SlideCanvasProps,
  ShareDialogProps,
} from './props.types';

